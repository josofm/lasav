package lasav_test

import (
	"testing"

	"github.com/stretchr/testify/assert"
)

func TestShouldTestTheTestCard(t *testing.T) {
	assert.Nil(t, nil, "Should be null!")
}

func TestShouldGetBestCardCorrectly(t *testing.T) {
	// card := lasav.NewCard()
	// fmt.Println(card)

	// urlGetCard := "bla" + "cards/637"
	// resp, err := http.Get(urlGetCard)
	// body := resp.Body
	// defer body.Close()
	// b := json.NewDecoder(body)
	// cards := make(map[string]interface{})
	// err = b.Decode(&cards)

	// fmt.Println(cards)

	assert.Nil(t, nil, "Should be nil!!")
	// assert.NotNil(t, cards, "Should be not nil!!")
}
